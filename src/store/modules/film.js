import api from '../api'
import * as types from '../types'

const state = {
  nowPlayingList: [],
  comingSoonList: [],
  nowPage: 0,
  comingPage: 0,
  getNowMore: true,
  getComingMore: true
}

const actions = {
  // 获取热映列表
  getNowPlayList: function () {
    
  }
}
